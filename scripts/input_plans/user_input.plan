# Drive the P1 profile off explicit foreground-handoff anchors so the same
# plan can run on both host and tcg without relying on prompt timing.
wait_for [USERINPUT] foreground -> hsh
wait 0.5
text hello
key ret

wait_for [USERINPUT] foreground -> calc
wait 0.5
text 3 4 +
key ret
